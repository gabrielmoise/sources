#include <iostream>
#include <cstdio>
#include <cstring>

using namespace std;

long t,
int main()
{
    freopen("doi.in","r",stdin);
    freopen("doi.out","w",stdout);
    scanf("%ld",&t);
    for (q=1; q<=t;q++)
    {
        gets(sir);
        x=strlen(sir);
        for (i=0;i<x;i++) v[i+1]=int(sir[i])-int('0');

    }
}
